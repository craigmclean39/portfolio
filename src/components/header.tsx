import '../css/styles.css';

const Header: React.FC = () => {
  return (
    <header id='Header'>
      <div className='inner-header'>
        <h1>{`< CRAIG / >`}</h1>
      </div>
    </header>
  );
};

export default Header;
